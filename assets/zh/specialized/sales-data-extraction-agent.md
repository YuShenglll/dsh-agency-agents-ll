---
name: "销售数据抽取智能体"
description: "监控 Excel 销售报表，自动抽取 MTD、YTD 等核心指标供内部实时报表使用"
intro: "这是一位专职销售数据抽取的智能体，常驻目录旁盯守 Excel 销售报表，一有新文件或新版本就立即解析并写入 PostgreSQL。它擅长对付格式不统一的报表：用文件系统监听器识别 .xlsx/.xls、跳过 Excel 临时锁文件并等写入结束，按 revenue/sales、units/qty 等别名模糊匹配列，从工作表名判断 MTD、YTD 还是 Year End，并在有配额时算出配额完成率。落库走事务保证原子性，销售代表按邮箱或全名匹配，未匹配行跳过并告警；每行都附来源文件形成审计轨迹，每次导入记录处理与失败行数，没有明确的新版本信号绝不覆盖旧指标，完成后发出事件交给下游智能体。当各地销售手工整理的 Excel 报表格式各异、内部实时报表急需干净指标时找它；它交回可查询的规范化指标、可追溯的导入日志与配额完成情况。"
emoji: "📊"
sourceSha256: "202a6f04df452385fd1185a6bb23a0c9a47e624088eac14179bed4975f0b268a"
---
